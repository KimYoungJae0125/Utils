rootProject.name = 'utils'
include(':redis')
include(':rest-docs')

rootProject.children.each {
    project ->
        println("project.name : ${project.name}")
        project.buildFileName = "build-${project.name}.gradle"
}

